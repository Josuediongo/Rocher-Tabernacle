<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Connexion | ROCHER TABERNACLE</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #3c3b6e, #00bcd4);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', sans-serif;
    }
    .card {
      border-radius: 1rem;
      padding: 2rem;
      background: #ffffffee;
      box-shadow: 0 10px 25px rgba(0,0,0,0.15);
      max-width: 420px;
      width: 100%;
    }
    .card img {
      max-height: 100px;
      margin-bottom: 1rem;
    }
    .card h4 {
      font-weight: 700;
      color: #3c3b6e;
    }
    .form-control {
      border: none;
      border-bottom: 2px solid #3c3b6e;
      border-radius: 0;
      background: transparent;
    }
    .form-control:focus {
      box-shadow: none;
      border-color: #00bcd4;
    }
    .btn-custom {
      background: #3c3b6e;
      color: white;
      width: 100%;
    }
    .btn-custom:hover {
      background: #2a2956;
    }
    .forgot-link {
      display: block;
      margin-top: 1rem;
      text-align: center;
      color: #3c3b6e;
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="card text-center">
    <img src="/images/loo5.jpg" alt="Logo Rocher Tabernacle" class="mx-auto d-block">
    <h4>Connexion à TAB ROCHER</h4>
    <form action="/login" method="POST" class="mt-4">
      <div class="mb-3">
        <input type="email" name="email" class="form-control" placeholder="Email" required>
      </div>
      <div class="mb-3">
        <input type="password" name="password" class="form-control" placeholder="Mot de passe" required>
      </div>
      <button type="submit" class="btn btn-custom">Se connecter</button>
      <a href="/reset-password" class="forgot-link">Mot de passe oublié ?</a>
    </form>
  </div>

</body>
</html>
